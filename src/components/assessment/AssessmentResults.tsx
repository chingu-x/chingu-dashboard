import TierDisplay from "./TierDisplay";

export default function AssessmentResultsPage() {
  return (
    <div className="flex min-h-screen w-full items-center justify-center overflow-hidden bg-white text-black">
      <TierDisplay />
    </div>
  );
}
